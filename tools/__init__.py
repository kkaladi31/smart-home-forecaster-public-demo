"""Data tools for the Smart-Home Forecaster.

Each tool is a plain Python function that returns a JSON-serializable dict.
Keeping them framework-free means you can test them directly (see the
`if __name__ == "__main__"` blocks) before any LLM/agent code is involved.
"""
